let age = 18;
let result1 = (age >= 18) ? "Eligible" : "Not Eligible";
console.log(result1);
//condition ? trueValue : falseValue;

//Nested Ternary
let score = 80;

let grade = score >= 90 ? "A"
          : score >= 75 ? "B"
          : score >= 50 ? "C"
          : "Fail";

console.log(grade);